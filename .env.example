# Docker Container Configuration
COMPOSE_PROJECT_NAME=containername

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=user
DB_PASSWORD=password
DB_NAME=academy_db

# DBMS Configuration
PMA_HOST=db

# Root Section
DB_ROOT_PASSWORD=rootpassword

# Timezone Configuration
TZ=timezone

# Application Environment
NODE_ENV=state

# Notes:
# - If running application locally with Docker DB, use DB_HOST=localhost
# - If running application inside Docker network, use DB_HOST=db (service name from compose.yaml)
# - DB_USER and DB_PASSWORD should match MYSQL_USER and MYSQL_PASSWORD from compose.yaml